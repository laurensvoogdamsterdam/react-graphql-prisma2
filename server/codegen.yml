overwrite: true
schema: "http://localhost:4000/"
documents: "./src/**/*.ts"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - typescript-resolvers
    config:
      useIndexSignature: false